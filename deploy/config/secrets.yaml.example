# Copy to secrets.yaml (chmod 600) on the dock Pi — NEVER commit the real one.
# The password is the LXC mosquitto "dock" account
# (/root/mowbot-mqtt-credentials.txt on the LXC).
username: dock
password: CHANGE-ME
